# 台灣茶葉網站 - 使用說明

## 📋 目錄
- [系統架構](#系統架構)
- [如何啟動網站](#如何啟動網站)
- [前台功能使用](#前台功能使用)
- [管理員功能](#管理員功能)
- [開發指令](#開發指令)
- [常見問題](#常見問題)

---

## 🏗️ 系統架構

### 技術堆疊
- **前端**: React 18 + Vite + React Router
- **後端**: Python FastAPI + Motor (MongoDB async driver)
- **資料庫**: MongoDB 6
- **容器化**: Docker + Docker Compose

### 專案結構
```
TAIWANTEA/
├── backend/              # 後端 API
│   ├── src/
│   │   ├── main.py      # FastAPI 應用程式入口
│   │   ├── config.py    # 設定檔
│   │   ├── models/      # MongoDB 資料模型
│   │   ├── routers/     # API 路由
│   │   ├── schemas/     # Pydantic 驗證結構
│   │   └── services/    # 商業邏輯
│   ├── .env             # 環境變數設定
│   └── requirements.txt # Python 套件清單
│
├── frontend/            # 前端網頁
│   ├── src/
│   │   ├── App.jsx      # React 主程式
│   │   ├── pages/       # 頁面元件
│   │   ├── components/  # UI 元件
│   │   ├── services/    # API 客戶端
│   │   └── hooks/       # React Hooks
│   ├── .env             # 環境變數
│   └── package.json     # NPM 套件清單
│
└── docker-compose.yml   # Docker 編排設定
```

---

## 🚀 如何啟動網站

### 方法一：使用 Docker Compose（推薦）

```bash
# 1. 複製環境變數檔案
cp backend/.env.example backend/.env
cp frontend/.env.example frontend/.env

# 2. 啟動所有服務（MongoDB + 後端 + 前端）
docker-compose up -d

# 3. 檢查服務狀態
docker-compose ps

# 4. 匯入初始資料（首次啟動需要）
docker-compose exec backend python -m src.scripts.seed_data
```

**服務網址：**
- 前台網站: http://localhost:5173
- 後端 API: http://localhost:8585
- API 文件: http://localhost:8585/docs

---

### 方法二：手動啟動（開發模式）

#### 第一步：啟動 MongoDB

```bash
# 使用 Docker 啟動 MongoDB
docker compose up -d mongodb

# 或使用系統的 MongoDB
mongod --dbpath /path/to/data
```

#### 第二步：啟動後端 API

```bash
cd backend

# 建立虛擬環境
uv venv

# 安裝套件
uv pip install -r requirements.txt

# 設定環境變數
cp .env.example .env
# 編輯 .env 檔案，設定 MONGODB_URL 和 SECRET_KEY

# 匯入初始資料（首次啟動）
source .venv/bin/activate
PYTHONPATH=. python src/scripts/seed_data.py

# 啟動 FastAPI 伺服器
uvicorn src.main:app --reload
```

後端 API 運行於：http://localhost:8585

#### 第三步：啟動前端網站

```bash
# 開啟新的終端機視窗
cd frontend

# 安裝 NPM 套件
npm install

# 設定環境變數
cp .env.example .env
# 編輯 .env，設定 VITE_API_URL=http://localhost:8585

# 啟動開發伺服器
npm run dev
```

前端網站運行於：http://localhost:5173

---

## 🌐 前台功能使用

### 瀏覽茶葉商品

1. **首頁瀏覽**
   - 開啟網站：http://localhost:5173
   - 會看到所有茶葉分類和商品

2. **商品分類**
   網站提供 6 種茶葉分類：
   - 🍃 **綠茶（Green Tea）**: 龍井、煎茶、抹茶
   - ☕ **紅茶（Black Tea）**: 阿薩姆、伯爵茶、錫蘭紅茶
   - 🌿 **烏龍茶（Oolong Tea）**: 阿里山烏龍、鐵觀音、東方美人
   - 🤍 **白茶（White Tea）**: 白毫銀針、白牡丹、月光白
   - 🌺 **花草茶（Herbal Tea）**: 洋甘菊、薄荷、南非國寶茶
   - 🍂 **普洱茶（Pu-erh Tea）**: 生普、熟普、10年陳年普洱

3. **商品資訊**
   每個商品卡片顯示：
   - 商品圖片
   - 茶葉名稱
   - 分類標籤
   - 詳細描述
   - 價格（新台幣）
   - 庫存狀態

4. **分類導航**
   - 點擊分類名稱可快速跳轉到該分類區塊
   - 支援平滑捲動效果

---

## 👨‍💼 管理員功能

### 登入管理後台

#### 預設管理員帳號
```
電子郵件: admin@taiwantea.com
密碼: Admin123!
```

### 使用 API 登入

```bash
# 方法一：使用 curl
curl -X POST http://localhost:8585/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@taiwantea.com","password":"Admin123!"}' \
  -c cookies.txt

# 方法二：使用 API 文件（Swagger UI）
# 開啟 http://localhost:8585/docs
# 找到 POST /api/auth/login
# 點擊 "Try it out"
# 輸入管理員帳密
# 點擊 "Execute"
```

### 管理 API 端點

#### 認證相關
- `POST /api/auth/login` - 管理員登入
- `POST /api/auth/logout` - 登出
- `GET /api/auth/me` - 取得當前管理員資訊

#### 商品管理（需要實作）
未來版本將提供：
- `POST /api/admin/products` - 新增商品
- `PUT /api/admin/products/{id}` - 更新商品
- `DELETE /api/admin/products/{id}` - 刪除商品
- `POST /api/admin/products/upload` - 上傳商品圖片

#### 分類管理（需要實作）
未來版本將提供：
- `POST /api/admin/categories` - 新增分類
- `PUT /api/admin/categories/{id}` - 更新分類
- `DELETE /api/admin/categories/{id}` - 刪除分類

---

## 💻 開發指令

### 後端指令

```bash
cd backend

# 啟動開發伺服器（支援熱重載）
uvicorn src.main:app --reload

# 執行測試
pytest

# 執行測試並顯示涵蓋率
pytest --cov=src

# 程式碼格式化
black src/

# 程式碼檢查
ruff check src/

# 匯入測試資料
PYTHONPATH=. python src/scripts/seed_data.py
```

### 前端指令

```bash
cd frontend

# 安裝套件
npm install

# 啟動開發伺服器
npm run dev

# 建置正式版本
npm run build

# 預覽建置結果
npm run preview

# 執行測試
npm test

# 程式碼檢查
npm run lint

# 程式碼格式化
npm run format
```

### Docker 指令

```bash
# 啟動所有服務
docker-compose up -d

# 停止所有服務
docker-compose down

# 查看服務狀態
docker-compose ps

# 查看服務日誌
docker-compose logs -f

# 查看特定服務日誌
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f mongodb

# 重新建置並啟動
docker-compose up -d --build

# 進入後端容器
docker-compose exec backend bash

# 進入 MongoDB 容器
docker-compose exec mongodb mongosh taiwantea
```

---

## 🔧 資料庫操作

### 使用 MongoDB Shell

```bash
# 連線到 MongoDB
docker exec -it taiwantea-mongodb mongosh taiwantea

# 查看所有分類
db.categories.find().pretty()

# 查看所有商品
db.products.find().pretty()

# 查看管理員帳號
db.admins.find().pretty()

# 計算商品數量
db.products.countDocuments()

# 查詢特定分類的商品
db.products.find({ category: "green-tea" }).pretty()

# 查詢特定價格範圍的商品
db.products.find({ price: { $gte: 20, $lte: 40 } }).pretty()

# 更新商品庫存狀態
db.products.updateOne(
  { _id: ObjectId("商品ID") },
  { $set: { inStock: false } }
)

# 刪除商品
db.products.deleteOne({ _id: ObjectId("商品ID") })
```

### 新增測試商品

```bash
docker exec taiwantea-mongodb mongosh taiwantea --eval '
db.products.insertOne({
  name: "測試茶葉",
  category: "green-tea",
  description: "這是一個測試商品",
  price: 25.99,
  imageUrl: "https://placehold.co/600x450/2d5016/FFFFFF/png?text=Test+Tea",
  thumbnailUrl: "https://placehold.co/300x225/2d5016/FFFFFF/png?text=Test+Tea",
  inStock: true,
  displayOrder: 99,
  createdAt: new Date(),
  updatedAt: new Date()
})
'
```

---

## ❓ 常見問題

### Q1: 網站無法啟動，顯示連線錯誤？

**A:** 確認 MongoDB 是否正在運行：

```bash
# 檢查 MongoDB 容器狀態
docker ps | grep mongo

# 如果沒有運行，啟動 MongoDB
docker compose up -d mongodb
```

### Q2: 前端看不到商品？

**A:** 檢查以下項目：

1. 確認後端 API 正常運行：
```bash
curl http://localhost:8585/api/products
```

2. 確認資料庫有資料：
```bash
docker exec taiwantea-mongodb mongosh taiwantea --eval 'db.products.countDocuments()'
```

3. 檢查瀏覽器控制台是否有錯誤訊息（F12 開啟開發者工具）

### Q3: 後端啟動失敗，顯示 SECRET_KEY 錯誤？

**A:** 檢查並設定環境變數：

```bash
cd backend
cat .env

# 確認 SECRET_KEY 是否設定（需要至少 32 個字元）
# 如果沒有，手動編輯 .env 檔案
nano .env
```

### Q4: Docker Compose 啟動很慢？

**A:** 第一次啟動需要下載 Docker 映像檔，可能需要幾分韘。後續啟動會快很多。

```bash
# 查看下載進度
docker-compose up

# 強制重新下載最新映像檔
docker-compose pull
```

### Q5: 如何重置資料庫？

**A:** 刪除 MongoDB 資料並重新匯入：

```bash
# 方法一：使用 Docker Volume
docker-compose down -v  # 刪除所有資料
docker-compose up -d    # 重新啟動
docker-compose exec backend python -m src.scripts.seed_data

# 方法二：手動刪除集合
docker exec taiwantea-mongodb mongosh taiwantea --eval '
db.products.deleteMany({});
db.categories.deleteMany({});
db.admins.deleteMany({});
'
```

### Q6: 埠號衝突，無法啟動？

**A:** 修改 docker-compose.yml 或啟動指令中的埠號：

```bash
# 後端使用 8586 而非 8585
uvicorn src.main:app --reload --port 8586

# 前端使用 5174 而非 5173
npm run dev -- --port 5174
```

### Q7: 如何查看伺服器日誌？

**A:**

```bash
# Docker 模式
docker-compose logs -f backend
docker-compose logs -f frontend

# 手動模式
# 後端日誌會直接顯示在終端機
# 前端日誌會顯示在瀏覽器控制台（F12）
```

---

## 📞 技術支援

### 專案文件
- 完整規格: `specs/001-i-wnat-to/spec.md`
- 實作計畫: `specs/001-i-wnat-to/plan.md`
- 任務清單: `specs/001-i-wnat-to/tasks.md`
- API 規範: `specs/001-i-wnat-to/contracts/api.yaml`
- 資料模型: `specs/001-i-wnat-to/data-model.md`

### 開發狀態
- 實作狀態: `IMPLEMENTATION_STATUS.md`
- 下一步驟: `NEXT_STEPS.md`

### 學習資源
- FastAPI 文件: https://fastapi.tiangolo.com/
- React 文件: https://react.dev/
- MongoDB 文件: https://www.mongodb.com/docs/
- Vite 文件: https://vitejs.dev/

---

## 🎯 下一步開發計畫

目前已完成：
- ✅ 專案設置和環境配置
- ✅ 資料庫架構和資料模型
- ✅ 後端 API（商品和分類）
- ✅ 前端使用者介面
- ✅ 管理員認證系統

待開發功能：
- ⏳ 管理後台 UI 介面
- ⏳ 商品管理功能（新增、編輯、刪除）
- ⏳ 圖片上傳功能
- ⏳ 分類導航選單
- ⏳ 購物車功能（未來版本）
- ⏳ 結帳流程（未來版本）

詳細開發計畫請參考：`specs/001-i-wnat-to/plan.md`

---

## 📝 版本資訊

**當前版本**: v1.0.0-beta
**最後更新**: 2025-10-09
**開發分支**: 001-i-wnat-to

**已完成進度**: 36/96 任務（38%）
- Phase 1: 專案設置 ✅ 100%
- Phase 2: 基礎架構 ✅ 100%
- Phase 3: 使用者故事 1（瀏覽商品）🚧 60%
- Phase 4: 使用者故事 2（分類導航）⏳ 待開始
- Phase 5: 使用者故事 3（管理後台）⏳ 待開始
- Phase 6: 優化和部署 ⏳ 待開始

---

祝您使用愉快！🍵
